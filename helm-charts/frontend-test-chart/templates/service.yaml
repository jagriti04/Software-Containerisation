apiVersion: v1
kind: Service
metadata:
  name: {{ include "frontend-test-chart.fullname" . }}
  labels:
    {{- include "frontend-test-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5000
      protocol: TCP
      name: http
  selector:
    {{- include "frontend-test-chart.selectorLabels" . | nindent 4 }}
