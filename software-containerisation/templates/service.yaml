apiVersion: v1
kind: Service
metadata:
  name: {{ include "software-containerisation.fullname" . }}
  labels:
    {{- include "software-containerisation.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "software-containerisation.selectorLabels" . | nindent 4 }}
