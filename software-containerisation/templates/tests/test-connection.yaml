apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "software-containerisation.fullname" . }}-test-connection"
  labels:
    {{- include "software-containerisation.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "software-containerisation.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
